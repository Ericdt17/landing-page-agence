import { Outlet } from "react-router-dom";

/** Passe-partout pour les routes `/offre/:jobId` et `/offre/:jobId/postuler`. */
const RecruitmentJobLayout = () => <Outlet />;

export default RecruitmentJobLayout;
